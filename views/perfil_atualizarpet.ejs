<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="utf-8" />
  <meta content="width=device-width, initial-scale=1.0" name="viewport" />
  <title><%= titulo %></title>

  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&amp;display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body class="bg-[#1F1F1F] text-white font-['Roboto']">

  <header class="bg-main-color w-full flex items-center justify-between px-0 fixed top-0 left-0 z-40">
      <!-- NAVEGAÇÃO -->
    <nav class="bg-main-color shadow-lg w-full">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">

            <div class="flex items-center"><img src="/images/logo-branco.png" class="w-20 tira-margem-padding" alt="Logo" /></div>
                <!-- Menu à esquerda -->

                <div class="flex space-x-8 items-center">
                    <a href="/home" class="text-white hover:bg-gray-700 px-3 py-2 rounded-lg text-base font-medium transition duration-200">Início</a>
                    <a href="/perfil" class="text-white hover:bg-gray-700 px-3 py-2 rounded-lg text-base font-medium transition duration-200">Perfil</a>
                    <a href="/categorias" class="text-white hover:bg-gray-700 px-3 py-2 rounded-lg text-base font-medium transition duration-200">Categorias</a>
                    <a href="/forum" class="text-white hover:bg-gray-700 px-3 py-2 rounded-lg text-base font-medium transition duration-200">Forum</a>
                </div>
                <!-- Nome do administrador à direita -->
                <div class="flex items-center">
                </div>
            </div>
        </div>
    </nav>

    <!-- Área de Pesquisa 
    <div class=>
      <input type="text" placeholder="Pesquisar..." class="px-4 py-2 rounded-lg text-white pesquisar w-64 relative left-[-50px] fundo-azul"/>
    </div>-->
  </header>

  <!-- Espaço do header fixo -->
  <div class="h-[100px]"></div>

   <!-- Formulário -->
  <section class="flex justify-center items-center py-10">
    <div class="bg-[#2a2a2a] p-8 rounded-lg w-full max-w-2xl">
      <h2 class="text-3xl font-bold mb-6 text-center">Atualizar Pet</h2>

      <% if (mensagem) { %>
        <div class="mb-4 p-3 rounded-md <%= sucesso ? 'bg-green-600' : 'bg-red-600' %> text-white">
          <%= mensagem %>
        </div>
      <% } %>

      <form action="/atualizarpet/<%= pet.id_pet %>" method="POST">
        <div class="mb-4">
          <label for="nome_pet" class="block text-white mb-2">Nome do Pet</label>
          <input type="text" id="nome_pet" name="nome_pet" value="<%= pet.nome_pet %>" class="w-full px-4 py-2 rounded bg-gray-700 text-white border border-gray-600" required>
        </div>

        <div class="mb-4">
          <label for="raca_pet" class="block text-white mb-2">Raça</label>
          <input type="text" id="raca_pet" name="raca_pet" value="<%= pet.raca_pet %>" class="w-full px-4 py-2 rounded bg-gray-700 text-white border border-gray-600" required>
        </div>

        <div class="mb-4">
          <label for="idade_pet" class="block text-white mb-2">Idade</label>
          <input type="number" id="idade_pet" name="idade_pet" value="<%= pet.idade_pet %>" class="w-full px-4 py-2 rounded bg-gray-700 text-white border border-gray-600" min="0" required>
        </div>

        <!-- Se for usar multer depois, pode adicionar um campo de imagem aqui -->

        <div class="flex justify-between mt-6">
          <a href="/perfil" class="bg-gray-600 hover:bg-gray-700 text-white font-semibold py-2 px-4 rounded-md transition duration-200">Cancelar</a>
          <button type="submit" class="bg-[#00e0d6] hover:bg-[#00bdb4] text-white font-semibold py-2 px-6 rounded-md transition duration-200">Salvar Alterações</button>
        </div>
      </form>
    </div>
  </section>

</body>
</html>
